<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= check_box_tag("completed", value= "0", checked=  false)  %><%= link_to task.title, task_path(task)%> <%= link_to "Delete", destroy_path(task), method: :delete %>
    </li>
  <%end %>
</ul>


